import { createCptListHandler } from "../../_shared/cpt-handler";
export const dynamic = "force-dynamic";

export const GET = createCptListHandler({
  postType: "knesset-of-customs",
  postTypeName: "knesset of customs",
});
